XML Pretty Print
------------------

Application to format XML. It was used to force standard formatting of XML files in a project to avoid noise in merges due to spaces and other formatting artifacts.
